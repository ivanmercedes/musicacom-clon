{% extends "partials/layout.twig" %}
 
{% block content %}
<section>
    <div class="bg-white p-4 shadow-sm mb-3">
       <h1 class="font-weight-bold h4 text-uppercase">GENEROS DE MÚSICA - {{genero.name}}</h1>
      <p>Canciones más visitadas del genero {{genero.name}}</p>
       <div class="row mt-5">
        <div class="col-md-7 row">
           {% for lyric in lyrics %}
             {% include "loop/list.twig" %}
           {% endfor %}
        </div>
        
             <div class="col-md-5 sidebar-ads p-0">
                <div class="sidebar w-100">
                    {{ads.vertical300 | raw}}
                 </div>
             </div>  
       </div>  
           
    </div>
</section>
{% endblock %}

{% block footer %}
{% endblock %}